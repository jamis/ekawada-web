<ul>
<% @figures.each do |figure| %>
  <li><%= link_to(figure.canonical_name, figure_path(figure)) %></li>
<% end %>
</ul>

<% if can_alter_data? %>
  <%= link_to("Add a new figure", new_figure_path) %>
<% end %>
